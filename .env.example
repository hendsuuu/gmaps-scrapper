# Example local .env file – copy to .env and fill in values
# Never commit the real .env to version control!

# Optional: path to proxies.txt (default: proxies.txt in project root)
# PROXIES_FILE=proxies.txt
